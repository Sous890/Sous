[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "alembic upgrade head && python seed_admin.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
